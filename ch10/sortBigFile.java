package ch10;

/**
 * Created by lipingxiong on 9/30/15.
*/

public class sortBigFile {

 /* 10.6
 use external sort.
 Three steps:
         1)divide the file into smaller chunks, x MB each, where x is the size of memory we have .
         2) each chunk is sorted seperatedly, and then saved back to file system
         3) merge the chunks, one by one.
 */
}
